{
  "name": "cryptobots",
  "version": "1.0.0",
  "description": "Scalable instaces for Pricetrackers discord bot.",
  "main": "index.js",
  "license": "MIT",
  "engines": {
    "node": "^16.11.10"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/node": "^16.11.10",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "axios": "^0.24.0",
    "discord.js-light": "^4.3.1",
    "dotenv": "^10.0.0"
  },
  "scripts": {
    "dev": "ts-node-dev --transpile-only --respawn --ignore-watch node_modules src/index.ts",
    "build": "rm -rf ./dist && tsc --build",    
    "start": "node ./dist/index.js"
  }
}
